# Aliases
#-------------------

# Commands
alias c="clear"
alias l="ls -lacF"
alias flushdns="sudo killall -HUP mDNSResponder"

# Directories
export rojo="$HOME/Dev/Rojo"
export tweek="$HOME/Dev/Tweek"
export ilv="$HOME/Dev/ILV"
export devops="$HOME/Dev/DevOps"
alias rojo-configs="cd $rojo/Github/configs && subl ."
alias rojo-github="cd $rojo/Github"
alias rojo-forks="cd $rojo/Forks"
alias rojo-playground="cd $rojo/Playground"

# Files
alias zshsource="source ~/.zshrc && source ~/.zpreztorc"
alias zshaliases="subl ~/.zprezto_aliases"
alias zshenv="subl ~/.zprezto_env"
alias zshopts="subl ~/.zprezto_opts"
alias zshconfig="subl ~/.zpreztorc"
alias theme="subl /Users/rojo/.zprezto/modules/prompt/functions/prompt_rojo_setup"
alias sshconfig="subl ~/.ssh/config"

# Rails
alias rs="rails server"
alias rc="rails console"

# Binds
#-------------------

bindkey -e
bindkey '^[[1;9C' forward-word
bindkey '^[[1;9D' backward-word

# Binds [Commented for now]
# # Who doesn't want home and end to work?
# bindkey '\e[1~' beginning-of-line
# bindkey '\e[4~' end-of-line

# # Incremental search is elite!
# bindkey -M vicmd "/" history-incremental-search-backward
# bindkey -M vicmd "?" history-incremental-search-forward

# # Search based on what you typed in already
# bindkey -M vicmd "//" history-beginning-search-backward
# bindkey -M vicmd "??" history-beginning-search-forward

# bindkey "\eOP" run-help

# # oh wow!  This is killer...  try it!
# bindkey -M vicmd "q" push-line

# # it's like, space AND completion.  Gnarlbot.
# bindkey -M viins ' ' magic-space

# Service control
#-------------------

# Redis
export redis_plist="/usr/local/opt/redis/homebrew.mxcl.redis.plist"
alias startredis="launchctl load $redis_plist"
alias stopredis="launchctl unload $redis_plist"

# Postgre
export postgre_plist="/usr/local/opt/postgresql/homebrew.mxcl.postgresql.plist"
alias startpostgre="launchctl load $postgre_plist"
alias stoppostgre="launchctl unload $postgre_plist"

# Mongo
export mongo_plist="/usr/local/opt/mongodb/homebrew.mxcl.mongodb.plist"
alias startmongo="launchctl load $mongo_plist"
alias stopmongo="launchctl unload $mongo_plist"

# Elasticsearch
export elastic_plist="/usr/local/opt/elasticsearch/homebrew.mxcl.elasticsearch.plist"
alias startelastic="launchctl load $elastic_plist"
alias stopelastic="launchctl unload $elastic_plist"
